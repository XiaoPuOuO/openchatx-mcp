{
  "name": "Shell",
  "description": "Non-interactive command execution and interactive pseudo-terminal access.",
  "enabled": true,
  "builtin": "shell",
  "tools": {
    "bash": {
      "enabled": true,
      "description": "Run a command in a fresh non-interactive host shell (zsh on macOS, PowerShell on Windows)."
    },
    "bash_process": {
      "enabled": true,
      "description": "List, read logs from, or stop bash processes launched with keep=true."
    },
    "terminal": {
      "enabled": true,
      "description": "Create, write, read, resize, or close an interactive pseudo-terminal."
    }
  },
  "skills": {}
}
